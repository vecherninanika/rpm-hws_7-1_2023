# RUN Flask:
python3.10 -m venv ./venv
. ./venv/bin/activate
pip install -r requirements.txt
python3.10 -m flask --app fl_pages.py --debug run

# Database:
docker run -d --name chat_multiclient \
-p 5656:5432 \
-e POSTGRES_USER=app \
-e POSTGRES_PASSWORD=change_me \
-e POSTGRES_DB=chat postgres

psql -h 127.0.0.1 -p 5656 -U app chat

create extension "uuid-ossp";

create table banned (id uuid primary key default uuid_generate_v4(),
                     name text not null);

insert into banned (name) values ('Maxeem'), ('Nika');
# почему я себя в бан добавила кто знает??

create table messages (username text not null,
                     message text not null);

######
docker start chat_multiclient 
python3.10 -m flask --app fl_pages.py --debug run